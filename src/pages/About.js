import React from "react";
import Nav from "../components/Nav";

const About = () => {
    return (
        <div>
            <Nav />
            ABout page
        </div>
    );
};

export default About;
